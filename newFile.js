{/* <script>
    function downloadDocument() {
        // Replace 'YOUR_DIRECT_DOWNLOAD_LINK' with the actual direct download link obtained from Google Drive
    }
    // Replace 'YOUR_DIRECT_DOWNLOAD_LINK' with the actual direct download link obtained from Google Drive
    var downloadLink = 'https://docs.google.com/document/d/1uh_l3ifDCfy9ljDgzK1wjaP2Vpz3sEAA4ADHAtbKfJ0/edit?usp=drive_link';

    // Create a temporary anchor element
    var a = document.createElement('a');
    a.href = downloadLink;
    a.download = 'document_name.pdf'; // Specify the desired file name with appropriate extension

    // Append the anchor element to the body
    document.body.appendChild(a);

    // Programmatically trigger a click event on the anchor element to start the download
    a.click();

    // Remove the anchor element from the DOM
    document.body.removeChild(a);
}
</script>; */}
